# Copy to deploy/.env on the box (gitignored) and fill in.
# Cloudflare Tunnel token: Zero Trust dashboard → Networks → Tunnels →
#   (your tunnel) → Install connector → copy the token from the docker command.
# Keep the real value out of git — .env is ignored; store a copy in your password manager.
CLOUDFLARE_TUNNEL_TOKEN=
